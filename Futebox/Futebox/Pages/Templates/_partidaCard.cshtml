@model Futebox.Pages.RodadasModel
@{
}
<style>

    .wm {
        position: fixed;
        width: 100%;
        height: 100%;
        opacity: .1;
        display: none;
        position: fixed;
        z-index: 99999;
        text-align: center;
    }

        .wm > img {
            height: 100%;
            width: auto !important;
        }

    .focus .wm {
        display: block;
    }

    .focus {
        position: fixed;
        top: 0;
        left: 0;
        background: rgb(53,173,41);
        background: linear-gradient(45deg, rgba(53,173,41,1) 0%, rgba(176,255,169,1) 48%, rgba(69,217,54,1) 100%);
        /*width: 1920px;
        height: 1080px;*/
        width: 100%;
        height: 100%;
        z-index: 9999;
    }

    img {
        width: 48px;
    }

    .focus img {
        width: 72px;
    }

    .focus td {
        font-size: 24px;
        font-weight: 600;
        border-color: #030303;
    }

        .focus td.fw-bold {
            font-size: 38px;
            font-weight: 900;
        }

    .focus .titulo {
        font-size: 108px;
    }

    .focus .sub {
        font-size: 72px;
    }

    .focus .legenda {
        font-size: 24px;
        font-weight: bold;
        padding-left: 8px;
    }

    .focus .name {
        font-size: 28px;
    }

    .focus .x {
        font-size: 108px;
        font-weight: 900;
    }

    .focus .card-footer, .focus .card-header {
        font-size: 22px;
    }
    .focus .card-body {
        min-height: 164px;
    }

    .focus .card {
        background: #ffffff5c !important;
    }

    .br {
        border-right: 1px solid #030303;
    }
    .sp {
        padding: 0 10px 0 10px;
    }
</style>
<div class="wm">
        <img src="~/img/logo-1.png" />
    </div>
<h1 class="fs-sport text-center sub">@(Model.rodadaFoco)<sup>a</sup> Rodada !</h1>
<h1 class="fs-sport text-center titulo">@Model.nomeCampeonato</h1>
<div class="row sp">
    @foreach (var p in Model.partidas)
    {
        <div class="col-3 mb-2">
            <div id="content" class="card text-center">
                <div class="card-header fs-sport f">
                    @(p.dataPartida.ToString("ddd, d MMM yyyy - HH:mm"))
                </div>
                <div class="card-body" id="bg">
                    <div class="d-flex justify-content-around flex-row align-items-left">
                        <div class="d-flex justify-content-between align-items-center fs-sport">
                            <div class="logo-l d-flex flex-column align-items-center">
                                <img src="@(p.timeMandante.ObterLogoLocal())" />
                                <div class="name name-l ml-5">
                                    @(p.timeMandante.nome)
                                </div>
                            </div>
                        </div>
                        @*<div class="fs-sport">x</div>*@
                        <div class="d-flex justify-content-between align-items-center fs-sport">
                            <div class="logo-r d-flex flex-column align-items-center">
                                <img src="@(p.timeVisitante.ObterLogoLocal())" />
                                <div class="name name-r ml-5">
                                    @(p.timeVisitante.nome)
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-footer fs-sport f">
                    estádio @p.estadio
                </div>
            </div>
        </div>
    }
</div>



@*@foreach (var p in Model.partidas)
    {
        <div class="col-3 mb-5">
            <div id="content" class="card text-center">
                <div class="wm">
                    <img src="~/img/logo-1.png" />
                </div>
                <div class="card-body" id="bg">
                    <div class="d-flex justify-content-between flex-column align-items-left">
                        <div class="d-flex justify-content-between align-items-center fs-sport">
                            <div class="logo-l d-flex flex-row align-items-center">
                                <img src="@(p.timeMandante.ObterLogoLocal())" />
                                <div class="name-l ml-5">
                                    @(p.timeMandante.nome)
                                </div>
                            </div>
                        </div>
                        <div class="d-flex justify-content-between align-items-center fs-sport">
                            <div class="logo-r d-flex flex-row align-items-center">
                                <img src="@(p.timeVisitante.ObterLogoLocal())" />
                                <div class="name-r ml-5">
                                    @(p.timeVisitante.nome)
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-footer text-muted fs-sport f">
                    @p.dataHoraDaPartida <br /> estádio @p.estadio
                </div>
            </div>
        </div>
    }*@





@*<style>


        .logo-l > img,
        .logo-r > img {
            width: 60px;
        }

        #content {
            width: 100%;
            height: 100%;
        }

        .wm {
            position: fixed;
            width: 100%;
            height: 100%;
            opacity: .1;
        }

            .wm > img {
                width: 100%;
                margin-top: 33%;
            }
    </style>
    <div class="row">
        <table class="table table-borderless">
            <thead>
                <tr>
                    <th width="30%"></th>
                    <th width="30%"></th>
                    <th width="20%"></th>
                    <th width="20%"></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var p in Model.partidas.Take(Model.partidas.Count / 2))
                {
                    <tr>
                        <td class="">
                            <div class="logo-l d-flex flex-row align-items-center justify-content-start">
                                <img src="@(p.timeMandante.ObterLogoLocal())" />
                                <div class="name-r ml-5">
                                    @(p.timeMandante.nome)
                                </div>
                            </div>
                        </td>
                        <td class="logo-r">
                            <div class="logo-l d-flex flex-row align-items-center justify-content-end">
                                <div class="name-r ml-5">
                                    @(p.timeVisitante.nome)
                                </div>
                                <img src="@(p.timeVisitante.ObterLogoLocal())" />
                            </div>
                        </td>
                        <td>
                            @p.dataPartida.ToString("D"), @p.dataPartida.ToString("dd MMMM YYYY") às @p.dataPartida.ToString("HH:mm")
                        </td>
                        <td>
                            @p.estadio
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>*@